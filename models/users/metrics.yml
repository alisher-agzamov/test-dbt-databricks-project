version: 2

semantic_models:
  - name: users_cleaned
    model: ref('users_cleaned')
    description: "Semantic model for cleaned users"
    entities:
      - name: user_id
        type: primary
        expr: id
    measures:
      - name: users          # 👈 соответствует метрике ниже
        description: "Count of users"
        agg: count
    dimensions:
      - name: birthdate
        type: time

metrics:
  - name: users
    description: Count of users
    type: simple
    label: Count of users
    type_params:
      measure:
        name: users           # 👈 должно совпадать с названием выше
        fill_nulls_with: 0
        join_to_timespine: false
        alias: user_count
