version: 2

metrics:
  - name: users_count
    model: ref('users_cleaned')
    label: "Users Count"
    description: "Users Count"
    type: count
    timestamp: birthdate
    time_grains: [day, week, month]
